name: Qodana

on:
  push:
    branches: ["main"]
  pull_request:

jobs:
  qodana:
    runs-on: ubuntu-latest
    steps:
      - name: Placeholder check
        run: |
          echo "Qodana scan is disabled for this repository."
          echo "This placeholder job exists to keep required status checks mergeable."
